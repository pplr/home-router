ListenAddress 10.0.0.1

# A non-login shell (`ssh -t home-router btop`) sources neither /etc/profile nor
# /etc/locale.conf, so set the locale here too — otherwise btop bails out with
# "No UTF-8 locale detected" on exactly that invocation.
SetEnv LANG=C.UTF-8
